apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: prod-
components:
  - ../component
secretGenerator:
  - name: creds
    literals:
      - password=s3cret
openapi:
  path: openapi/schema.json
crds:
  - crds/myresource.yaml
resources:
  - deployment.yaml
