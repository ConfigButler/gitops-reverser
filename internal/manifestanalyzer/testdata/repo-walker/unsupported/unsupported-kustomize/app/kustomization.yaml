apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: prod-
components:
  - ../component
secretGenerator:
  - name: creds
    literals:
      - password=s3cret
resources:
  - deployment.yaml
