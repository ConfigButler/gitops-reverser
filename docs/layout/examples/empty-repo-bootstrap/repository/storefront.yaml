apiVersion: config.shop.example/v1alpha1
kind: ShopConfiguration
metadata:
  name: storefront
spec:
  catalog:
    defaultCurrency: EUR
  checkout:
    sessionTimeout: 15m
  theme:
    primaryColor: teal
