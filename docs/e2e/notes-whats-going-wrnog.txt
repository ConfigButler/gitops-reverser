Ok I tried to find the solution but failed. Somehow the label is dissapearing and we don't know anymore what to delete.

Perhaps I should go a bit smarter about this: or more pragmatic. I could also have the same namespace, always, for now. And just make sure that I have a task that always first deltes the old namespace that would also prevent me from sitting in this rabbit hole.

Let's celebrate the good things that come from this.

Let's move the gitea installation a layer higher: which will also be a timesaver

Let's move the gitea creation of credentials a level higher, let's be more clear on what to expect.

LEt's finally get that structure in the end2end tests, so that we can run things in parallel (and also write tests that mimic what could happen during a well visited talk!)


---

What I could wish:

1. I want a flexible way to verify that my code is behaving like it should.
2. I like speed, so I don't want to wait on useless stuff
3. My controller is complex: it requires an audit webhook in the kubernets cluster
4. I had the dream to run multiple tests in parallel: because of 3 I can't at the moment (could use something to distribute the events but thats to much for now)
    * Yesterday I realised that what I'm trying now is to complex, it's not needed to install in multiple namespaces. I can't keep the installations
5. An important thing is: I keep the running application, so if the test fails I can investigate the state afterwards
6. I have automated image updating and I have nice Makefile targets that will 'reinstall' when needed (e.g. config/ folder changes)
7. I can be flexible in how I install, so I can also switch to helm and see if it behaves the same.


What I learned:
* Makefile is file based, especially the dependency tree is very cool but also fragile if you are managing Kubernetes clusters with it. 
* Idea: what could GitOps do in this? Details do matter in this.
