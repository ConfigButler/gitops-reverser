# Local-only environment overrides for the Taskfile (loaded via `dotenv`).
# Copy this file to `.env` and adjust the values. `.env` is gitignored, so it
# never gets committed.

# Target image for `task docker-push-dev` (build + push to your own registry).
DEV_IMG=registry.example.com/gitops-reverser:latest
