{{- if gt (int .Values.replicaCount) 1 -}}
{{- fail "gitops-reverser runs a single replica: set .Values.replicaCount to 1. Standby replicas need ownership coordination first, so that two instances never write the same GitTarget." -}}
{{- end -}}
