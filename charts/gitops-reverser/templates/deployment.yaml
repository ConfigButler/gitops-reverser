apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gitops-reverser.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gitops-reverser.labels" . | nindent 4 }}
    app.kubernetes.io/component: manager
    app.kubernetes.io/created-by: gitops-reverser
    app.kubernetes.io/part-of: gitops-reverser
    control-plane: controller-manager
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "gitops-reverser.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "gitops-reverser.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "gitops-reverser.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: 20
      containers:
        - name: manager
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --health-probe-bind-address=:8081
            - --metrics-bind-address={{ .Values.servers.metrics.bindAddress }}
            {{- if not .Values.servers.metrics.tls.enabled }}
            - --metrics-insecure
            {{- end }}
            - --metrics-cert-path={{ .Values.servers.metrics.tls.certPath }}
            - --metrics-cert-name={{ .Values.servers.metrics.tls.certName }}
            - --metrics-cert-key={{ .Values.servers.metrics.tls.certKey }}
            {{- if not .Values.servers.admission.tls.enabled }}
            - --webhook-insecure
            {{- end }}
            - --webhook-cert-path={{ .Values.servers.admission.tls.certPath }}
            - --webhook-cert-name={{ .Values.servers.admission.tls.certName }}
            - --webhook-cert-key={{ .Values.servers.admission.tls.certKey }}
            {{- if and .Values.servers.audit.enabled (not .Values.servers.audit.tls.enabled) }}
            - --audit-insecure
            {{- end }}
            - --audit-listen-address={{ .Values.servers.audit.listenAddress }}
            - --audit-port={{ .Values.servers.audit.port }}
            - --audit-max-request-body-bytes={{ .Values.servers.audit.maxRequestBodyBytes }}
            - --audit-read-timeout={{ .Values.servers.audit.timeouts.read }}
            - --audit-write-timeout={{ .Values.servers.audit.timeouts.write }}
            - --audit-idle-timeout={{ .Values.servers.audit.timeouts.idle }}
            {{- if and .Values.servers.audit.enabled .Values.servers.audit.tls.enabled }}
            - --audit-cert-path={{ .Values.servers.audit.tls.certPath }}
            - --audit-cert-name={{ .Values.servers.audit.tls.certName }}
            - --audit-cert-key={{ .Values.servers.audit.tls.certKey }}
            {{- end }}
            {{- if .Values.logging.level }}
            - --zap-log-level={{ .Values.logging.level }}
            {{- end }}
            {{- if .Values.logging.development }}
            - --zap-development={{ .Values.logging.development }}
            {{- end }}
            {{- if .Values.logging.encoder }}
            - --zap-encoder={{ .Values.logging.encoder }}
            {{- end }}
            {{- if .Values.logging.stacktraceLevel }}
            - --zap-stacktrace-level={{ .Values.logging.stacktraceLevel }}
            {{- end }}
            {{- if .Values.webhook.audit.debugDumps }}
            - --audit-dump-path=/var/run/audit-dumps
            {{- end }}
          ports:
            - name: admission
              containerPort: {{ .Values.servers.admission.port }}
              protocol: TCP
            {{- if .Values.servers.audit.enabled }}
            - name: audit
              containerPort: {{ .Values.servers.audit.port }}
              protocol: TCP
            {{- end }}
            - name: metrics
              containerPort: {{ .Values.servers.metrics.port }}
              protocol: TCP
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.webhook.audit.debugDumps }}
            - name: audit-dumps
              mountPath: /var/run/audit-dumps
            {{- end }}
            - name: tmp-dir
              mountPath: /tmp
            {{- if .Values.servers.admission.tls.enabled }}
            - name: admission-cert
              mountPath: {{ .Values.servers.admission.tls.certPath }}
              readOnly: true
            {{- end }}
            {{- if and .Values.servers.audit.enabled .Values.servers.audit.tls.enabled }}
            - name: audit-cert
              mountPath: {{ .Values.servers.audit.tls.certPath }}
              readOnly: true
            {{- end }}
            {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        {{- if .Values.webhook.audit.debugDumps }}
        - name: audit-dumps
          emptyDir: {}
        {{- end }}
        - name: tmp-dir
          emptyDir: {}
        {{- if .Values.servers.admission.tls.enabled }}
        - name: admission-cert
          secret:
            secretName: {{ include "gitops-reverser.fullname" . }}-admission-server-cert
            defaultMode: 420
        {{- end }}
        {{- if and .Values.servers.audit.enabled .Values.servers.audit.tls.enabled }}
        - name: audit-cert
          secret:
            secretName: {{ .Values.servers.audit.tls.secretName | default (printf "%s-audit-server-cert" (include "gitops-reverser.fullname" .)) }}
            defaultMode: 420
        {{- end }}
        {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
