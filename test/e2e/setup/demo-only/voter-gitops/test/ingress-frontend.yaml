apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-static
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`demo-test.configbutler.ai`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: voter-frontend
          port: http
          scheme: http
