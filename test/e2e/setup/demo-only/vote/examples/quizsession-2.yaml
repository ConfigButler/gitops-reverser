apiVersion: examples.configbutler.ai/v1alpha1
kind: QuizSession
metadata:
  name: a-second-one
  namespace: vote
spec:
  title: This is another question
  state: live
  questions:
    - id: q1
      type: singleChoice
      title: How do you make simple YAML changes?
      required: true
      choices:
        - "Create a PR"
        - "Rolled something myself"
        - "Different"
    