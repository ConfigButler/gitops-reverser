apiVersion: configbutler.ai/v1alpha2
kind: GitProvider
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  url: {{.RepoURL}}
  allowedBranches:
    - "{{.Branch}}"
  push:
    commitWindow: "{{.CommitWindow}}"
  secretRef:
    name: {{.SecretName}}
