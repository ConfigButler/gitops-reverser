apiVersion: configbutler.ai/v1alpha1
kind: GitTarget
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
spec:
  provider:
    kind: GitProvider
    name: {{ .RepoConfigName }}
  branch: {{ .Branch }}
  path: {{ .BaseFolder }}
