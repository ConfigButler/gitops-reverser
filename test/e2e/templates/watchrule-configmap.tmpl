apiVersion: configbutler.ai/v1alpha1
kind: WatchRule
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  target:
    kind: GitTarget
    name: {{.DestinationName}}
  rules:
  - resources: ["configmaps"]
