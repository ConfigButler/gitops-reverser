apiVersion: configbutler.ai/v1alpha1
kind: WatchRule
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
spec:
  destinationRef:
    name: {{ .DestinationName }}
  rules:
  - resources: ["icecreamorders"]