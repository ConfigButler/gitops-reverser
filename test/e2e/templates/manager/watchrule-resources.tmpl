apiVersion: configbutler.ai/v1alpha3
kind: WatchRule
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  targetRef:
    kind: GitTarget
    name: {{.DestinationName}}
  rules:
  - resources: [{{.Resources}}]
