apiVersion: v1
kind: Service
metadata:
  name: monitoring
  namespace: platform
  labels:
    app.kubernetes.io/name: monitoring
spec:
  selector:
    app.kubernetes.io/name: monitoring
  ports:
    - name: metrics
      port: 9091
      targetPort: metrics
