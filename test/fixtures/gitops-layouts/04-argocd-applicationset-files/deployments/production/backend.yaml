# ApplicationSet generator input. This is NOT a Kubernetes object: it has no
# apiVersion, no kind, and no metadata. It is plain data consumed by the git
# files generator in applicationset.yaml.
name: backend
namespace: backend
chartVersion: 1.4.2
values:
  replicas: 3
  image: ghcr.io/example-org/backend:2.4.0
