# NOT YAML. Plain key=value lines consumed by secretGenerator envs:.
# Deliberately fake placeholder values - this is a fixture, not real secrets.
DATABASE_PASSWORD=not-a-real-password
SESSION_SIGNING_KEY=not-a-real-key
