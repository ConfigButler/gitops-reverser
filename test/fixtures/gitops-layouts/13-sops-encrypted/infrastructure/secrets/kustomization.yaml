# kustomize build config (not a cluster object). Passes the encrypted Secret
# through as-is; nothing here can read it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - db-credentials.enc.yaml
