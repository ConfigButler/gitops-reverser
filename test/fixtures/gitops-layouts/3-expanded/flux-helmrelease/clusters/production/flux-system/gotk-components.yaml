# This file is generated by flux bootstrap. DO NOT EDIT.
# ---------------------------------------------------------------------------
# TRUNCATED FOR THIS FIXTURE. The real gotk-components.yaml is ~35,000 lines:
# every Flux CRD (GitRepository, HelmRepository, OCIRepository, HelmRelease,
# Kustomization, ...), the RBAC (ServiceAccounts, ClusterRoles,
# ClusterRoleBindings) and the controller Deployments (source-controller,
# kustomize-controller, helm-controller, notification-controller). Only two
# representative objects are kept below so the fixture stays readable.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: flux-system
  labels:
    app.kubernetes.io/instance: flux-system
    app.kubernetes.io/part-of: flux
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helm-controller
  namespace: flux-system
  labels:
    app.kubernetes.io/component: helm-controller
    app.kubernetes.io/instance: flux-system
    app.kubernetes.io/part-of: flux
    control-plane: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helm-controller
  template:
    metadata:
      labels:
        app: helm-controller
    spec:
      serviceAccountName: helm-controller
      containers:
        - name: manager
          image: ghcr.io/fluxcd/helm-controller:v1.1.0
          ports:
            - name: http-prom
              containerPort: 8080
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
