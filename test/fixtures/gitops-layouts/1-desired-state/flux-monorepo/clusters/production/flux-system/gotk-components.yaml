# This file is generated by flux bootstrap. DO NOT EDIT.
# ---------------------------------------------------------------------------
# TRUNCATED FOR THIS FIXTURE. The real gotk-components.yaml is ~35,000 lines:
# every Flux CRD (GitRepository, Kustomization, HelmRelease, ...), the RBAC
# (ServiceAccounts, ClusterRoles, ClusterRoleBindings) and the controller
# Deployments (source-controller, kustomize-controller, helm-controller,
# notification-controller). Only two representative objects are kept below so
# the fixture stays readable.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: flux-system
  labels:
    app.kubernetes.io/instance: flux-system
    app.kubernetes.io/part-of: flux
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: source-controller
  namespace: flux-system
  labels:
    app.kubernetes.io/component: source-controller
    app.kubernetes.io/instance: flux-system
    app.kubernetes.io/part-of: flux
    control-plane: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: source-controller
  template:
    metadata:
      labels:
        app: source-controller
    spec:
      serviceAccountName: source-controller
      containers:
        - name: manager
          image: ghcr.io/fluxcd/source-controller:v1.4.1
          ports:
            - name: http
              containerPort: 9090
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
