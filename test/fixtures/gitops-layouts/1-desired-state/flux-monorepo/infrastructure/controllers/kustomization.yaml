# Kustomize build file (kustomize.config.k8s.io), NOT a cluster object.
# Aggregates the controller sources/releases that must reconcile before apps.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingress-nginx.yaml
