apiVersion: configbutler.ai/v1alpha1
kind: GitProvider
metadata:
  name: playground-provider
  namespace: tilt-playground
spec:
  url: http://gitea-http.gitea-e2e.svc.cluster.local:13000/testorg/playground.git
  allowedBranches:
    - "main"
  push:
    commitWindow: "0s"
  secretRef:
    name: git-creds-playground
