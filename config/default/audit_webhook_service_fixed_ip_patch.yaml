# Patch to set fixed ClusterIP for dedicated audit webhook service.
# This is required because kube-apiserver starts before CoreDNS
# and cannot rely on Service DNS resolution during bootstrap.
apiVersion: v1
kind: Service
metadata:
  name: audit-webhook-service
  namespace: system
spec:
  clusterIP: 10.96.200.200
