---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: gitrepoconfigs.configbutler.ai.configbutler.ai
spec:
  group: configbutler.ai.configbutler.ai
  names:
    kind: GitRepoConfig
    listKind: GitRepoConfigList
    plural: gitrepoconfigs
    singular: gitrepoconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitRepoConfig is the Schema for the gitrepoconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GitRepoConfig
            properties:
              branch:
                description: Branch is the Git branch to commit to.
                type: string
              push:
                description: Push defines the strategy for pushing commits to the
                  remote.
                properties:
                  interval:
                    description: |-
                      Interval is the maximum time to wait before pushing queued commits.
                      Defaults to "1m".
                    type: string
                  maxCommits:
                    description: |-
                      MaxCommits is the maximum number of commits to queue before pushing.
                      Defaults to 20.
                    type: integer
                type: object
              repoUrl:
                description: RepoURL is the URL of the Git repository to commit to.
                type: string
              secretName:
                description: SecretName is the name of the secret containing Git credentials.
                type: string
              secretNamespace:
                description: SecretNamespace is the namespace of the secret containing
                  Git credentials.
                type: string
            required:
            - branch
            - repoUrl
            - secretName
            - secretNamespace
            type: object
          status:
            description: status defines the observed state of GitRepoConfig
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
